Notable todo items list:
1. forced parameters for functions where necessary
2. remove temp buffers
3. avoid using useState when it rerenders entire page frequently, use useRef instead and manually trigger re-render, see below.
4. try avoid refreshing entire components ondrag
5. validation of function parameters
6. Fix trigger_rerender - DONE
7. Add note latest location cordinates to db, update position on drag
8. generic popup for active toolbar item
9. Select zoom in feature
10. Generic item bounds feature when active/selected
11. top right appbar options: history, present, share ...6
12. hungarian notation for all variables e.g. b_has_data for boolean
13. resize arrow on screen resize


/**** sample code for triggering useRef using useState
 * generally avoid using useState as it rerenders the entire page frequently on state change
 * if necessary to use useState, please trigger manually.
 ****/

import { useRef, useState } from "react";

const MyComponent = () => {
  const bufferRef = useRef(new ArrayBuffer(1024 * 1024)); // Large buffer
  const [, forceRender] = useState(0); // Dummy state

  const updateBuffer = () => {
    bufferRef.current = new ArrayBuffer(1024 * 1024); // Update ref
    forceRender((prev) => prev + 1); // Force re-render
  };

  return (
    <div>
      <p>Buffer updated!</p>
      <button onClick={updateBuffer}>Update Buffer</button>
    </div>
  );
};
